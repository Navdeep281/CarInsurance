@model IEnumerable<CarInsurance.Models.Insuree>

@{
    ViewBag.Title = "Admin";
}
<link rel="stylesheet" href="~/wwwroot/css/adminStyle.css" asp-append-version="true"/>

<h2>Admin</h2>

<table class=" = "table">
    <thread>
        <tr>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Email</th>
            <th>Quote</th>
        </tr>
    </thread>
    <tbody>
        @foreach (var insuree in Model)
            {
                <tr>
                    <td>@insuree.FirstName</td>
                    <td>@insuree.LastName</td>
                    <td>@insuree.Email</td>
                    <td>@insuree.Quote</td>
                </tr>
            }
    </tbody>
</table>

